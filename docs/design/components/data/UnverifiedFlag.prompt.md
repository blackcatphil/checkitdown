# UnverifiedFlag

Every number is either verified with a date or tilde'd and marked unverified.
There is no third option and nothing is ever stated as fact without a source.

An unverified figure is **shown, never hidden** — but it cannot win a ranking.
Pair it with `ExclusionNote`, which names the room and the reason.
