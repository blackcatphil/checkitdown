export interface EditorialReadProps {
  /** e.g. "Typically runs" / "Often cancels" / "Series only". */
  value: string;
  note?: string;
}
export declare function EditorialRead(props: EditorialReadProps): JSX.Element;
