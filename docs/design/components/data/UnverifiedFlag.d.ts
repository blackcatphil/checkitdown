export interface UnverifiedFlagProps {
  /** The figure WITHOUT the tilde — the component adds it. */
  value: string;
}
export declare function UnverifiedFlag(props: UnverifiedFlagProps): JSX.Element;
