# DataRow

The spec-pair primitive. Label (mono, dim) / value (paper) / provenance (mono, dim).

Sections carry their own freshness because ages differ — cash-game specs are weekly,
promos daily. A single page-level "verified" date would be the average of things that
are not comparable.
