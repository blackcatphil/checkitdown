# QuietAction

Sentence-case underlined text. Everything that is not the one accent action.
Carries a 44px hit height even though it reads as text.

Set `external` for anything leaving the product (a room's own structure PDF,
a restaurant menu) — the arrow is how we keep our own links honest.
